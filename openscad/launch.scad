include <./KeyV2/includes.scad>

//support
$stem_type = "rounded_cherry";
$stabilizer_type = "cherry_stabilizer";

// sizing
$total_depth = 8.3;

// key shape
$top_tilt = 0;
$top_skew = 0;

//legend
$inset_legend_depth = 0.15;
